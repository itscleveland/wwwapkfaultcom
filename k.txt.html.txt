<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Draggable Console with Help Menu</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin-top: 50px;
            transition: background 0.5s;
        }

        button {
            background-color: #333;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            cursor: pointer;
            border-radius: 5px;
            margin: 5px;
        }

        button:hover {
            background-color: #555;
        }

        /* Help Dropdown */
        .dropdown {
            position: relative;
            display: inline-block;
        }

        .dropdown-content {
            display: none;
            position: absolute;
            background-color: white;
            min-width: 200px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
            z-index: 1;
            text-align: left;
            padding: 10px;
            border-radius: 5px;
        }

        .dropdown-content p {
            margin: 5px 0;
            font-size: 14px;
        }

        .dropdown:hover .dropdown-content {
            display: block;
        }

        /* Console Box */
        .console {
            position: fixed;
            bottom: 20px;
            right: 20px;
            width: 300px;
            height: 200px;
            background: black;
            color: limegreen;
            padding: 10px;
            border-radius: 8px;
            display: none;
            box-shadow: 0px 0px 10px rgba(0, 255, 0, 0.5);
            font-family: monospace;
            resize: both;
            overflow: auto;
        }

        .console-header {
            background: limegreen;
            color: black;
            padding: 5px;
            cursor: grab;
            text-align: center;
            font-weight: bold;
            border-radius: 5px 5px 0 0;
        }

        .console-input {
            width: 100%;
            background: black;
            color: limegreen;
            border: none;
            outline: none;
            font-family: monospace;
            padding: 5px;
            font-size: 14px;
        }
    </style>
</head>
<body>

    <button id="toggleConsole">Toggle Console</button>

    <!-- Help Dropdown -->
    <div class="dropdown">
        <button>Help Menu ▼</button>
        <div class="dropdown-content">
            <p><b>search [query]</b> → Google Search</p>
            <p><b>bg [color]</b> → Change Background Color</p>
            <p><b>goto [website]</b> → Open a Website</p>
            <p><b>time</b> → Show Current Time</p>
            <p><b>hack</b> → Fake Hacking Animation</p>
            <p><b>premium</b> → Open xx.66ghz.com</p>
        </div>
    </div>

    <div id="console" class="console">
        <div id="consoleHeader" class="console-header">Draggable Console</div>
        <input type="text" id="consoleInput" class="console-input" placeholder="Type a command...">
    </div>

    <script>
        let consoleBox = document.getElementById("console");
        let consoleHeader = document.getElementById("consoleHeader");
        let consoleInput = document.getElementById("consoleInput");

        document.getElementById("toggleConsole").addEventListener("click", function () {
            consoleBox.style.display = consoleBox.style.display === "block" ? "none" : "block";
            consoleInput.focus();
        });

        // Command Handling
        consoleInput.addEventListener("keydown", function (event) {
            if (event.key === "Enter") {
                let command = consoleInput.value.trim().toLowerCase();

                if (command.startsWith("search ")) {
                    let query = command.substring(7);
                    window.open(`https://www.google.com/search?q=${encodeURIComponent(query)}`, "_blank");
                } else if (command.startsWith("bg ")) {
                    let color = command.substring(3);
                    document.body.style.background = color;
                } else if (command.startsWith("goto ")) {
                    let url = command.substring(5);
                    if (!url.startsWith("http")) url = "http://" + url;
                    window.open(url, "_blank");
                } else if (command === "time") {
                    alert("Current Time: " + new Date().toLocaleString());
                } else if (command === "hack") {
                    consoleBox.style.background = "red";
                    consoleInput.value = "Hacking in progress...";
                    setTimeout(() => {
                        consoleBox.style.background = "black";
                        consoleInput.value = "";
                        alert("ACCESS GRANTED: You are now inside the system! 🕶️");
                    }, 3000);
                } else if (command === "premium") {
                    window.open("http://xx.66ghz.com", "_blank");
                } else {
                    alert("Unknown command: " + command);
                }

                consoleInput.value = ""; // Clear input
            }
        });

        // Make the console draggable
        consoleHeader.onmousedown = function (event) {
            let shiftX = event.clientX - consoleBox.getBoundingClientRect().left;
            let shiftY = event.clientY - consoleBox.getBoundingClientRect().top;

            function moveAt(pageX, pageY) {
                consoleBox.style.left = pageX - shiftX + "px";
                consoleBox.style.top = pageY - shiftY + "px";
            }

            function onMouseMove(event) {
                moveAt(event.pageX, event.pageY);
            }

            document.addEventListener("mousemove", onMouseMove);

            consoleHeader.onmouseup = function () {
                document.removeEventListener("mousemove", onMouseMove);
                consoleHeader.onmouseup = null;
            };
        };

        consoleHeader.ondragstart = function () {
            return false;
        };
    </script>

</body>
</html>
